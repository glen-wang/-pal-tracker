---
applications:
  - name: pal-tracker
    path: build/libs/pal-tracker.jar
    random-route: true
    env:
      WLECOME_MESSAGE: 'Helloo from Cloud Foundry'
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ } }'

